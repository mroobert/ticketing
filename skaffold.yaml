apiVersion: skaffold/v2beta10
kind: Config
deploy:
  kubectl:
    manifests:
      - ./infra/k8s/*
build:
  local:
    push: false
  artifacts:
    - image: mroobert/auth-service
      context: auth
      docker: 
        dockerfile: Dockerfile
      sync:
        manual:
          - src: 'auth/src/**/*.ts'
            dest: .
